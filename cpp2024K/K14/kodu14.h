#ifndef KODU14_H
#define KODU14_H

#include <vector>
using namespace std;

template <typename T>
void liida(vector<T>& v, T& lisa);

template <typename T>
void vali(vector<T>& v, T a, T b, vector<T>& v1);

#endif